using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Esercizio : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Se eseguo in start, allora si stampa solo quando premo play sull'editor
        Debug.Log("Hello World");
    }

    // Update is called once per frame
    void Update()
    {
        //Se eseguo in update, allora si stampa ad ogni frame da quando premo play sull'editor
        //Debug.Log("Hello World");
    }
}
